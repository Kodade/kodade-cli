# unverified: sourced from https://ampcode.com/manual (CLI not installed locally)
# Identification only: no screen rules until a real session confirms its prompt strings.
# No title matcher: the short title substring would false-positive (case-sensitive).
name = "amp"
display = "Amp"
process = ["amp"]
