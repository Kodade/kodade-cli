# unverified: sourced from https://github.com/QwenLM/qwen-code (CLI not installed locally)
# Identification only: no screen rules until a real session confirms its prompt strings.
name = "qwen-code"
display = "Qwen Code"
process = ["qwen"]
title = ["Qwen"]
