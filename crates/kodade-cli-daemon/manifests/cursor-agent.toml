# unverified: sourced from https://cursor.com/docs/cli/overview (CLI not installed locally)
# Identification only: no screen rules until a real session confirms its prompt strings.
name = "cursor-agent"
display = "Cursor CLI"
process = ["cursor-agent"]
title = ["Cursor Agent", "Cursor CLI"]
